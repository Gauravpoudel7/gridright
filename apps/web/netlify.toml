# Netlify build config for the GridRight web app (monorepo: apps/web).
# The Next.js runtime plugin handles server components, server actions,
# and routing — no publish directory needed.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
